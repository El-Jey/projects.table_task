// import jwt_decode from "jwt-decode";

// export default {
//     isBearerTokenExpired(token) {
//         const { exp } = jwt_decode(token);

//         return Math.floor(Date.now() / 1000) > exp;
//     }
// };